# Dependabot config: monthly, grouped updates.
version: 2
updates:
  # Keep the Actions pinned in .github/workflows up to date.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns:
          - "*"
